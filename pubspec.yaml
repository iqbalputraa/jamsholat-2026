name: jadwal_sholat_app
description: Aplikasi jadwal sholat menggunakan API Waktu Sholat Indonesia
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  geolocator: ^13.0.0
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  just_audio: ^0.9.40

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/